# Pinned versions (update after re-export)

| Component | Version / note |
|-----------|----------------|
| ComfyUI | Track git commit used when workflows were exported |
| Custom nodes (SDXL IP-Adapter) | Record in commit message when workflows change |
| Z-Image Turbo template | ComfyUI docs tutorial baseline |
| FLUX loaders | ComfyUI built-in FLUX support |

Re-export all `{family}.api.json` files when upgrading ComfyUI or custom nodes.
